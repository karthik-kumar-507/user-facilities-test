<div class="card">
  <div class="card-header text-center">
    <h3>Edit User Form</h3>
  </div>
  <div class="card-body">
    <%= render 'form', url: user_path(@user.id), http_method: 'put' %>
    <%= link_to 'Cancel', users_path %>
  </div>
</div>

